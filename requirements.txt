# Dependencies for the three model-based MARL baselines: MAG, MAG_2 (= MAMBA), MABL.
# These exact versions were verified running all three repos together on Rorqual
# (StdEnv/2023, python/3.10) on 2026-08-31. See setup_information.md for the full
# install procedure -- this file is NOT meant to be fed to pip in one shot, because
# the packages come from three different sources on an Alliance cluster.
#
# The per-repo requirements.txt files inside MAG/, MAG_2/ and MABL/mabl/ are stale
# (numpy 1.18 / torch 1.7 / ray 1.5) -- ignore them; the repos only use stable APIs
# and run fine on the versions below.

# --- Group 1: Alliance wheelhouse (install with: pip install --no-index ...) ---
numpy==1.25.2
torch==2.5.0
ray==2.2.0
matplotlib==3.7.2
more-itertools==9.1.0
setproctitle==1.3.4
pygame==2.5.2
protobuf==3.20.3
absl-py==1.4.0
grpcio==1.51.3

# --- Group 2: PyPI, not in the wheelhouse (install on a LOGIN node, which has internet) ---
PySC2==4.0.0
s2clientprotocol==5.0.14.93333.0

# --- Group 3: oxwhirl SMAC (StarCraft Multi-Agent Challenge), from git, pinned commit ---
# WARNING: do NOT run `pip install smac` or `pip install --no-index smac`.
# Both PyPI and the Alliance wheelhouse resolve "smac" to SMAC3, an unrelated
# AutoML package. The one below is the correct one.
SMAC @ git+https://github.com/oxwhirl/smac.git@12614f1760427026cce82083dc3f0ab3ff1d939e

# --- Group 4: optional, only if enabling wandb logging (setup_information.md, section 6) ---
# wandb        # available in the wheelhouse: pip install --no-index wandb
